# Copy this file to vars.dev.sh and edit the variables, then load it in the current shell with
#   . vars.dev.sh
#
# In production, use the relevant system to set these variables in the host's environment

export DATABASE_URL="/tmp/alayatodo.db"
export SECRET_KEY="choose a better key"
